import 'package:flutter/material.dart';

class GraduationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('GraduationScreen',
          style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
    );
  }
}
